package core.gridParser.grids

object GridOffsets {

//  override def getLocation(offset: Int): Location = {
//    val searchResult = util.Arrays.binarySearch(rowOffsets, offset)
//    if (searchResult >= 0) {
//      Location(searchResult, 0)
//    } else {
//      val row = searchResult * -1 - 2
//      Location(row, offset - rowOffsets(row))
//    }
//  }
//
//  override def get(offset: Int): Option[T] = {
//    get(getLocation(offset))
//  }
}
