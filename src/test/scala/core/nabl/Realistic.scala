package core.nabl

import org.scalatest.FunSuite

class Realistic extends FunSuite with LanguageWriter {

  test("list with map") {
//    val nilDef = Struct("nilS", Seq.empty)
//    val consDef = Struct("consS", Seq(Field("value", "a"), Field("tail", LanguageTypeApplication("list", "a"))))
//    val listDef = UnionType("list", Seq(UnionOption("nil", "nilS"), UnionOption("cons", "consS")))
//    val mapDef = Lambda("f", Lambda("xs"))
  }
}
